rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Cho phép đọc và ghi cho tất cả người dùng đã xác thực
      allow read, write: if request.auth != null;
    }
  }
}
